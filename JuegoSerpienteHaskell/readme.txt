# HaskellSnakeGame

creditos @leonardo-ono
contacto al20760255@ite.edu.mx

crear una carpeta ejemplo y entrar en ella
correr el comando cabal init
checar que este corriendo la app Main.hs usando cabal run
despues entrar en la carpeta app
correr el comando cabal install gloss
si error de .cabal instalar sudo apt-get install freeglut3 freeglut3-dev
regresar a carpeta ejemplo y correr cabal install --lib gloss
hacer cabal run
